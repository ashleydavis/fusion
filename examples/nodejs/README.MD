A simple example of using Fusion in TypeScript under Node.js.

This example shows how a logging service can be automatically injected into another class.

To run this:

    cd fusion/examples/nodejs
    npm install
    npm start